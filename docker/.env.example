# Copy this file to docker/.env and set a strong password before running docker compose.
# docker/.env is gitignored — never commit the real password.
MSSQL_SA_PASSWORD=change_me_strong_password_here
